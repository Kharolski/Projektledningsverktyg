<Page x:Class="Projektledningsverktyg.Views.Tasks.Components.Meals.MealsTab"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Projektledningsverktyg.Views.Tasks.Components.Meals"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MealsTab">

    <Grid x:Name="MainGrid" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Controls -->
        <StackPanel Grid.Row="0" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Left" 
                    Margin="0,0,0,20">
            <ComboBox Width="120" Margin="0,0,10,0">
                <ComboBoxItem Content="Alla måltider"/>
                <ComboBoxItem Content="Vegetariskt"/>
                <ComboBoxItem Content="Veganskt"/>
                <ComboBoxItem Content="Glutenfritt"/>
            </ComboBox>
            <TextBox Width="200" 
                     Padding="10,5"
                     VerticalContentAlignment="Center"
                     Background="#F5F5F5">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Style.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <VisualBrush Stretch="None" AlignmentX="Left">
                                            <VisualBrush.Visual>
                                                <TextBlock Text="Sök måltider..." Foreground="Gray"/>
                                            </VisualBrush.Visual>
                                        </VisualBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </StackPanel>

        <!-- Add Meal Section -->
        <local:AddMealSection Grid.Row="1"/>

        <!-- Meals List -->
        <local:ListMealsSection Grid.Row="2"/>
        
    </Grid>
</Page>
